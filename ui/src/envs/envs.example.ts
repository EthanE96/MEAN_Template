export const environment = {
  // Envs example
  production: false,
  // This secret should be injected at build time workflow as Github Secrets
  clarityProjectId: '',
  apiUrl: '',
  documentationUrl: '',
};
